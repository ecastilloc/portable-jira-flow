---
name: portable-jira-flow-v2
description: Opt-in behavior-contract pilot for portable-jira-flow in Codex. Use when the user invokes $portable-jira-flow-v2 for doctor, specify, trace, or shadow planning.
---

# Portable Jira Flow V2 For Codex

Generated by portable-jira-flow installer.

Use this adapter when the user invokes `$portable-jira-flow-v2`.

Read the canonical v2 skill:

```text
{{SKILL_ROOT}}/v2/SKILL.md
```

Then follow the v2 reference routing. V2 shares the same local config and selected profiles as the stable `portable-jira-flow` skill, while writing separate v2 run artifacts.

Treat `config.defaults.json` as the executable generic base. Treat `config.example.json` and `config.local.example.jsonc` as templates only. Preserve the shared safety contract: never print secrets or private local config values, never publish or mutate environments without explicit allowed intent, and keep `run.json` authoritative for execution state.

Codex invocation examples:

```text
$portable-jira-flow-v2 doctor
$portable-jira-flow-v2 specify ABC-123
$portable-jira-flow-v2 trace ABC-123
```
